<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
    <channel>
        <title>Posts on Magsther&#39;s Blog</title>
        <link>https://magsther.github.io/posts/</link>
        <description>Recent content in Posts on Magsther&#39;s Blog</description>
        <generator>Hugo -- gohugo.io</generator>
        <language>en-us</language>
        <copyright>&lt;a href=&#34;https://creativecommons.org/licenses/by-nc/4.0/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;CC BY-NC 4.0&lt;/a&gt;</copyright>
        <lastBuildDate>Thu, 14 Jul 2022 15:47:55 +0200</lastBuildDate>
        <atom:link href="https://magsther.github.io/posts/index.xml" rel="self" type="application/rss+xml" />
        
        <item>
            <title>Elastic Cloud on Kubernetes</title>
            <link>https://magsther.github.io/posts/elastic/</link>
            <pubDate>Thu, 14 Jul 2022 15:47:55 +0200</pubDate>
            
            <guid>https://magsther.github.io/posts/elastic/</guid>
            <description>What is Elastic Cloud? Elastic Cloud is the best way to consume all of Elastic&amp;rsquo;s products across any cloud. Easily deploy in your favorite public cloud, or in multiple clouds, and extend the value of Elastic with cloud-native features.
What is Kubernetes? Kubernetes, often abbreviated as “K8s”, orchestrates containerized applications to run on a cluster of hosts. The K8s system automates the deployment and management of cloud native applications using on-premises infrastructure or public cloud platforms.</description>
            <content type="html"><![CDATA[<h1 id="what-is-elastic-cloud">What is Elastic Cloud?</h1>
<p><em>Elastic Cloud is the best way to consume all of Elastic&rsquo;s products across any cloud. Easily deploy in your favorite public cloud, or in multiple clouds, and extend the value of Elastic with cloud-native features.</em></p>
<h1 id="what-is-kubernetes">What is Kubernetes?</h1>
<p><em>Kubernetes, often abbreviated as “K8s”, orchestrates containerized applications to run on a cluster of hosts. The K8s system automates the deployment and management of cloud native applications using on-premises infrastructure or public cloud platforms.</em></p>
<h1 id="elastic-cloud-on-kubernetes">Elastic Cloud on Kubernetes</h1>
<p>We will use our <a href="https://magsther.github.io/posts/kind/">Kind</a> that we deployed earlier.</p>
<p>Elastic has created a good <a href="https://www.elastic.co/guide/en/cloud-on-k8s/current/k8s-quickstart.html">quickstart</a> on how to <strong>manually</strong> deploy <strong>Elastic Cloud on Kubernetes</strong> (short ECK).</p>
<p>We are all for <strong>automation</strong> ;) so we will do it with <a href="https://www.terraform.io/">Terraform</a></p>
<p>In a previous <a href="https://magsther.github.io/posts/kubeprometheus/">post</a>, we deployed <a href="https://github.com/prometheus-operator/kube-prometheus">Kube-Prometheus</a> by creating a Terraform module.</p>
<p>Let&rsquo;s create another module for <strong>Elastic Cloud</strong>.</p>
<h3 id="elastic-cloud---provider">Elastic Cloud - Provider</h3>
<p>As always, when working with <strong>Terraform providers</strong>, we will first take a look at the <a href="https://registry.terraform.io/providers/elastic/ec/latest">Terraform Registry</a>.</p>
<h3 id="authentication">Authentication</h3>
<p>We need to tell Terraform how to authenticate with Elastic.</p>
<p>The <a href="https://registry.terraform.io/providers/elastic/ec/latest/docs">documentation</a> mentions that the provider <em>offers two methods of authentication against the remote API: apikey or a combination of username and password.</em></p>
<p>Using an <strong>API key</strong> is recommended, so that&rsquo;s what we will use here.</p>
<p><em>After you&rsquo;ve generated your API Key, you can make it available to the Terraform provider by exporting it as the environment variable EC_API_KEY (recommended)</em></p>
<p><code>export TF_VAR_ec_api_key=&quot;&lt;apikey value&gt;&quot;</code></p>
<h3 id="elastic-module">Elastic Module</h3>
<p>Create a new folder in the <strong>modules</strong> folder.
<code>mkdir modules/elastic-cloud</code></p>
<p>First we will add the Elastic Cloud provider to our <code>providers.tf</code> file</p>
<pre tabindex="0"><code>terraform {
  required_providers {
    ec = {
      source = &#34;elastic/ec&#34;
      version = &#34;0.4.1&#34;
    }
  }
}
</code></pre><h3 id="adding-elastic-resources">Adding Elastic resources</h3>
<p>Create a <code>main.tf</code> file.
In this file, we will create the <strong>Elastic resources</strong> that we want to use.</p>
<p>You can find some examples <a href="https://registry.terraform.io/providers/elastic/ec/latest/docs/resources/ec_deployment">here</a></p>
<p>In our case, we will use <strong>elasticsearch</strong> and <strong>kibana</strong>.
This deployment will be created in <strong>Azure</strong>.</p>
<p>The <code>ec_deployment</code> creates  <strong>elasticsearch</strong> and <strong>kibana</strong>.</p>
<pre tabindex="0"><code>resource &#34;ec_deployment&#34; &#34;elastic_deployment&#34; {
  name                   = var.ec_instance_name
  region                 = var.region
  deployment_template_id = var.deployment_template_id
  version                = var.es_version

  elasticsearch {
    autoscale = var.autoscale
  }

  kibana {}
}
</code></pre><p>As you can see from the snippet above, we make use of <strong>input variables</strong>, so that we can customize behavior.</p>
<p>The <code>deployment_template_id</code> and the <code>region</code> can be found <a href="https://www.elastic.co/guide/en/cloud/current/ec-regions-templates-instances.html">here</a></p>
<p><code>Autocaling</code> is set to false by default, but can be enabled by setting the value in <code>variables.tf</code> to <code>true</code>.</p>
<h3 id="adding-variables">Adding variables</h3>
<p>We will define the values in the <code>variables.tf</code> file.
Using variables allows us to write configuration that is flexible and easier to re-use.</p>
<pre tabindex="0"><code>variable &#34;ec_instance_name&#34; {
  type        = string
  default     = &#34;my-test-instance&#34;
  description = &#34;Name of Elastic deployment&#34;
}

variable &#34;region&#34; {
  type        = string
  default     = &#34;azure-westeurope&#34;
  description = &#34;Region of Elastic deployment&#34;
}

variable &#34;es_version&#34; {
  type        = string
  default     = &#34;8.2.3&#34;
  description = &#34;Version of Elastic deployment&#34;
}

variable &#34;deployment_template_id&#34; {
  type        = string
  default     = &#34;azure-memory-optimized&#34;
  description = &#34;Azure SKU of Elastic deployment&#34;
}

variable &#34;autoscale&#34; {
  default     = false
  type        = bool
  description = &#34;Autocale enabled of Elastic deployment&#34;
}
</code></pre><p>We also add an <code>output.tf</code> file. This will make it easier for us to connect to the cluster once it is deployed.</p>
<pre tabindex="0"><code>output &#34;elasticsearch_endpoint&#34; {
  value = ec_deployment.elastic_deployment.elasticsearch[0].https_endpoint
}

output &#34;elasticsearch_username&#34; {
  value = ec_deployment.elastic_deployment.elasticsearch_username
}

output &#34;elasticsearch_password&#34; {
  value     = ec_deployment.elastic_deployment.elasticsearch_password
  sensitive = true
}
</code></pre><p>In the <strong>root module</strong> update the <code>main.tf</code> file with our new module</p>
<pre tabindex="0"><code>module &#34;elastic&#34; {
  source     = &#34;./modules/elastic-cloud&#34;
}
</code></pre><h3 id="deploy-time">Deploy time</h3>
<p>Let&rsquo;s see if this works.</p>
<p>Run <code>terraform init</code> in your terminal.
This will download our new provider (elastic cloud)</p>
<p>Run <code>terraform plan</code> to see what will be deployed.</p>
<p>Run <code>terraform apply</code> to apply the plan. Respond to the confirmation prompt with a yes.</p>
<p>After a little while, the Elastic cluster is deployed and can be found in the <strong>Elastic Cloud portal</strong>.</p>
<h3 id="additional-resources">Additional resources</h3>
<p><a href="https://registry.terraform.io/providers/elastic/ec/latest/docs">https://registry.terraform.io/providers/elastic/ec/latest/docs</a></p>
]]></content>
        </item>
        
        <item>
            <title>Kube-Prometheus (with Terraform and Helm)</title>
            <link>https://magsther.github.io/posts/kubeprometheus/</link>
            <pubDate>Mon, 11 Jul 2022 20:37:53 +0200</pubDate>
            
            <guid>https://magsther.github.io/posts/kubeprometheus/</guid>
            <description>Deploying Kube-Prometheus using Terraform (and Helm) In the previous post we deployed our Kind cluster using Terraform.
Let&amp;rsquo;s continue and add the kube-prometheus stack to our Kubernetes cluster.
What is Kube-Prometheus? Kube-Prometheus is magic ;)
You deploy it to your Kubernetes cluster and it will automatically collect metrics from all Kubernetes components. This means, prometheus , some default set of rules and pre-configured Grafana dashboards!!!
The only prerequisites we need is a Kubernetes cluster, which we deployed in our previosu post.</description>
            <content type="html"><![CDATA[<h2 id="deploying-kube-prometheus-using-terraform-and-helm">Deploying Kube-Prometheus using Terraform (and Helm)</h2>
<p>In the previous post we deployed our <strong>Kind</strong> cluster using <strong>Terraform</strong>.</p>
<p>Let&rsquo;s continue and add the <a href="https://github.com/prometheus-operator/kube-prometheus">kube-prometheus</a> stack to our Kubernetes cluster.</p>
<h2 id="what-is-kube-prometheus">What is Kube-Prometheus?</h2>
<p><strong>Kube-Prometheus</strong> is magic ;)</p>
<p>You deploy it to your Kubernetes cluster and it will automatically collect metrics from all Kubernetes components.
This means, <strong>prometheus</strong> , some default set of <strong>rules</strong> and pre-configured <strong>Grafana</strong> dashboards!!!</p>
<p>The only prerequisites we need is a Kubernetes cluster, which we deployed in our previosu post.</p>
<h2 id="terraform-providers-and-authentication">Terraform providers and authentication</h2>
<p>While we could deploy <strong>kube-prometheus</strong> by running <code>helm install my-kube-prometheus-stack prometheus-community/kube-prometheus-stack --version 36.6.2</code>, we will create a <strong>terraform</strong> modules for this.</p>
<p>Before creating our new modules, we need to add the following providers to our root module.</p>
<ul>
<li><a href="https://registry.terraform.io/providers/hashicorp/helm/latest/docs">Helm</a></li>
<li><a href="https://registry.terraform.io/providers/hashicorp/kubernetes/latest/docs">Kubernetes</a></li>
</ul>
<blockquote>
<p>Remember, if you are going to use <strong>providers</strong> (like <strong>helm</strong>, <strong>kubernetes</strong>), you will need to add them to your code.</p>
</blockquote>
<p>while you could add this to the <code>main.tf</code> file, it&rsquo;s usually better to seperate it into it&rsquo;s own file.</p>
<p>Add this content to <code>provders.tf</code></p>
<pre tabindex="0"><code># For authentication, the Helm provider can get its configuration by supplying a path to your kubeconfig file.
provider &#34;helm&#34; {
  kubernetes {
    config_path = &#34;~/.kube/config&#34;
  }
}

# Used to interact with the resources supported by Kubernetes.
# The provider needs to be configured with the proper credentials before it can be used.
provider &#34;kubernetes&#34; {
  config_path = pathexpand(var.kube_config)
}
</code></pre><p>Create a new file called <code>variables.tf</code> with this content:</p>
<pre tabindex="0"><code>variable &#34;kube_config&#34; {
  type    = string
  default = &#34;~/.kube/config&#34;
}
</code></pre><h2 id="create-the-terraform-module">Create the Terraform module</h2>
<p>Now, we can start with our module.</p>
<p>Create a new folder called <code>modules</code> and inside that a new folder with the name of your module. In our case, this will be <code>kube-prometheus</code>.</p>
<p>Create a new file in that folder and name it <code>main.tf</code></p>
<p>Add this content to that file:</p>
<pre tabindex="0"><code>resource &#34;helm_release&#34; &#34;kube-prometheus&#34; {
  name       = &#34;kube-prometheus-stackr&#34;
  namespace  = var.namespace
  version    = var.kube-version
  repository = &#34;https://prometheus-community.github.io/helm-charts&#34;
  chart      = &#34;kube-prometheus-stack&#34;
}
</code></pre><p>How did we know which <strong>helm chart</strong> to use?
Well, we went to <a href="https://artifacthub.io/">artifacthub</a> which is a web-based application that contains <strong>Kubernetes</strong> packages.</p>
<p>We searched for <strong>kube-prometheus</strong>, so we put that in the search bar, which gave us information how to deploy it using <a href="https://artifacthub.io/packages/helm/prometheus-community/kube-prometheus-stack">Helm</a></p>
<p>Next, we went to <a href="https://registry.terraform.io/providers/hashicorp/helm/latest/docs">Terraform Registry</a> to look up the <strong>Helm</strong> provider.</p>
<h2 id="namespaces">Namespaces</h2>
<p>Creating namespace with the Kubernetes provider is better than auto-creation in the helm_release.</p>
<p>Create a new file  called <code>namespaces.tf</code> with the following content:</p>
<pre tabindex="0"><code>resource &#34;kubernetes_namespace&#34; &#34;monitoring&#34; {
  metadata {
    name = var.namespace
  }
}
</code></pre><p>In the <code>variables.tf</code> file we will add our input variables.</p>
<p>Note, here we set the default value of our namespace to <strong>monitoring</strong></p>
<pre tabindex="0"><code>variable &#34;kube_config&#34; {
  type    = string
  default = &#34;~/.kube/config&#34;
}

variable &#34;namespace&#34; {
  type    = string
  default = &#34;monitoring&#34;
}

variable &#34;kube-version&#34; {
}
</code></pre><p>The last step we need to do is to &ldquo;call&rdquo; our module from the <code>main.tf</code> file in the root module.</p>
<pre tabindex="0"><code>module &#34;kube&#34; {
  source = &#34;./modules/kube-prometheus&#34;
  kube-version = &#34;36.2.0&#34;
}
</code></pre><h2 id="deploying-using-terraform">Deploying using Terraform</h2>
<p>Since we added a new module, we need to run <code>terraform init</code> to install this since its required by the configuration.</p>
<p>If we now run <code>terraform plan</code>, it should detect our changes.</p>
<pre tabindex="0"><code>...
....
# module.kube.kubernetes_namespace.monitoring will be created
  + resource &#34;kubernetes_namespace&#34; &#34;monitoring&#34; {
      + id = (known after apply)

      + metadata {
          + generation       = (known after apply)
          + name             = &#34;monitoring&#34;
          + resource_version = (known after apply)
          + uid              = (known after apply)
        }
    }
</code></pre><p>Apply the terraform plan with <code>terraform apply</code></p>
<p>After a little while <strong>kube-prometheus</strong> is installed to our <strong>Kubernetes</strong> cluster.</p>
<h2 id="interacting-with-kube-prometheus">Interacting with Kube-Prometheus</h2>
<p>Let&rsquo;s have a look of what was deployed in the previous step.</p>
<p>First, check what <strong>pods</strong> are running:</p>
<p><code>kubectl get pods -n monitoring</code></p>
<pre tabindex="0"><code>NAME                                                        READY   STATUS    RESTARTS   AGE
alertmanager-kube-prometheus-stackr-alertmanager-0          2/2     Running   0          66m
kube-prometheus-stackr-grafana-7cd9d66c5c-rhxsm             3/3     Running   0          66m
kube-prometheus-stackr-kube-state-metrics-98cbcdf4b-2x8qp   1/1     Running   0          66m
kube-prometheus-stackr-operator-7bbddd8bb4-b9pcv            1/1     Running   0          66m
kube-prometheus-stackr-prometheus-node-exporter-njjx6       1/1     Running   0          66m
kube-prometheus-stackr-prometheus-node-exporter-p8nnq       1/1     Running   0          66m
prometheus-kube-prometheus-stackr-prometheus-0              2/2     Running   0          66m
</code></pre><p>Let&rsquo;s check what <strong>services</strong> are deployed:</p>
<p><code>kubectl get svc --namespace monitoring</code></p>
<pre tabindex="0"><code>NAME                                              TYPE        CLUSTER-IP      EXTERNAL-IP   PORT(S)                      AGE
alertmanager-operated                             ClusterIP   None            &lt;none&gt;        9093/TCP,9094/TCP,9094/UDP   69m
kube-prometheus-stackr-alertmanager               ClusterIP   10.96.49.165    &lt;none&gt;        9093/TCP                     70m
kube-prometheus-stackr-grafana                    ClusterIP   10.96.91.188    &lt;none&gt;        80/TCP                       70m
kube-prometheus-stackr-kube-state-metrics         ClusterIP   10.96.38.109    &lt;none&gt;        8080/TCP                     70m
kube-prometheus-stackr-operator                   ClusterIP   10.96.44.188    &lt;none&gt;        443/TCP                      70m
kube-prometheus-stackr-prometheus                 ClusterIP   10.96.168.194   &lt;none&gt;        9090/TCP                     70m
kube-prometheus-stackr-prometheus-node-exporter   ClusterIP   10.96.131.191   &lt;none&gt;        9100/TCP                     70m
prometheus-operated                               ClusterIP   None            &lt;none&gt;        9090/TCP                     69m
</code></pre><h3 id="accessing-prometheus">Accessing Prometheus</h3>
<p>To access <strong>Prometheus</strong>, we can use <strong>port forwarding</strong>.</p>
<p><code>kubectl port-forward</code> allows using resource name, such as a pod name, to select a matching pod to port forward to.</p>
<p><code>kubectl port-forward svc/kube-prometheus-stackr-prometheus 9090:9090 --namespace monitoring</code></p>
<p><code>Forwarding from 127.0.0.1:9090 -&gt; 9090</code></p>
<p>Open a browser and go to: <code>http://localhost:9090/</code> to see the Prometehus UI.</p>
<h3 id="accessing-grafana">Accessing Grafana</h3>
<p>Similar, to access <strong>Grafana</strong>, use this command:</p>
<p><code>kubectl port-forward svc/kube-prometheus-stackr-grafana 3000:80 --namespace monitoring</code></p>
<h3 id="accessing-alertmanager">Accessing Alertmanager</h3>
<p><code>kubectl port-forward svc/kube-prometheus-stackr-alertmanager 9093:9093 --namespace monitoring</code></p>
]]></content>
        </item>
        
        <item>
            <title>Kind &amp; Terraform</title>
            <link>https://magsther.github.io/posts/kind/</link>
            <pubDate>Sat, 09 Jul 2022 19:50:17 +0200</pubDate>
            
            <guid>https://magsther.github.io/posts/kind/</guid>
            <description>What is Kind? kind is a tool for running local Kubernetes clusters using Docker container “nodes”. kind was primarily designed for testing Kubernetes itself, but may be used for local development or CI.
This is the beginning of a series of articles on how to manage Kubernetes resources via Terraform.
Deploying Kind using Terraform First we need a Kubernetes cluster running locally. There are many tools to pick from (minikube, K3s, MicroK8s etc.</description>
            <content type="html"><![CDATA[<h2 id="what-is-kind">What is Kind?</h2>
<p><em><a href="https://kind.sigs.k8s.io/">kind</a> is a tool for running local Kubernetes clusters using Docker container “nodes”.
kind was primarily designed for testing Kubernetes itself, but may be used for local development or CI.</em></p>
<p>This is the beginning of a series of articles on how to manage <strong>Kubernetes</strong> resources via <strong>Terraform</strong>.</p>
<h2 id="deploying-kind-using-terraform">Deploying Kind using Terraform</h2>
<p>First we need a <strong>Kubernetes</strong> cluster running locally.
There are many tools to pick from (<strong>minikube</strong>, <strong>K3s</strong>, <strong>MicroK8s</strong> etc..) , but the tool we will use is <strong>Kind</strong>.
One of the things that I really like with Kind, is the support of multi-node (including HA) clusters.</p>
<p>Note: To use kind, you will need to have <a href="https://docs.docker.com/get-docker/">docker</a> installed.</p>
<p>Even though we can easily create a new cluster by running <code>kind create cluster</code> we will do this by using <strong>Terraform</strong></p>
<p>Using <a href="https://www.terraform.io/">Terraform</a> to manage your Kubernetes resources have benefits like:</p>
<ul>
<li>Unified Workflow</li>
<li>Full Lifecycle Management</li>
<li>Graph of Relationships</li>
</ul>
<h2 id="terraform-registry">Terraform Registry</h2>
<p>The best place to start when using Terraform <strong>providers</strong> is the <strong>Terraform Registry</strong>.
Since we want to use <strong>Kind</strong>, we search for <a href="https://registry.terraform.io/providers/tehcyx/kind/0.0.13">Kind</a></p>
<p>The <strong>Use Provider</strong> button (at the top right corner), shows an example of how to use the provider.</p>
<p>We can use this as a template.</p>
<h2 id="setting-up-the-structure">Setting up the structure</h2>
<p>Create a new folder on your workstation:
<code>mkdir kind-demo</code></p>
<p>Create a <code>main.tf</code>. This file will hold the configuraton of the <strong>Kind</strong> cluster.</p>
<p>As you can see from the code below, our cluster will contain one node with the role <strong>control-plane</strong> and one node simply called <strong>worker</strong>.</p>
<p>Paste the following code:</p>
<pre tabindex="0"><code>terraform {
  required_providers {
    kind = {
      source = &#34;tehcyx/kind&#34;
      version = &#34;0.0.13&#34;
    }
  }
}

provider &#34;kind&#34; {}

  resource &#34;kind_cluster&#34; &#34;default&#34; {
    name = &#34;kindcluster&#34;
    node_image = &#34;kindest/node:v1.24.0&#34;
    wait_for_ready = true
    
    kind_config {
      kind = &#34;Cluster&#34;
      api_version = &#34;kind.x-k8s.io/v1alpha4&#34;

      node {
        role = &#34;control-plane&#34;
      }

      node {
        role = &#34;worker&#34;
      }
    }
  }
</code></pre><p>If you want to add additional nodes, you can just add them to the code like this:</p>
<pre tabindex="0"><code>    node {
      role = &#34;worker&#34;
    }
</code></pre><p>Save the <code>main.tf</code> file and in the terminal, type <code>terraform init</code></p>
<p>You should see an output similar to this:</p>
<pre tabindex="0"><code>Initializing the backend...

Initializing provider plugins...
- Finding tehcyx/kind versions matching &#34;0.0.13&#34;...
- Installing tehcyx/kind v0.0.13...
- Installed tehcyx/kind v0.0.13
...
Terraform has been successfully initialized!
</code></pre><p>Now run <code>terraform plan</code>.</p>
<p>The <strong>plan</strong> creates an execution plan, which lets you preview the changes that Terraform plans to make to your infrastructure.</p>
<pre tabindex="0"><code>Terraform will perform the following actions:

  # kind_cluster.default will be created
  + resource &#34;kind_cluster&#34; &#34;default&#34; {
      + client_certificate     = (known after apply)
      + client_key             = (known after apply)
      + cluster_ca_certificate = (known after apply)
      + completed              = (known after apply)
      + endpoint               = (known after apply)
      + id                     = (known after apply)
      + kubeconfig             = (known after apply)
      + kubeconfig_path        = (known after apply)
      + name                   = &#34;kindcluster&#34;
      + node_image             = &#34;kindest/node:v1.24.0&#34;
      + wait_for_ready         = true

      + kind_config {
          + api_version = &#34;kind.x-k8s.io/v1alpha4&#34;
          + kind        = &#34;Cluster&#34;

          + node {
              + role = &#34;control-plane&#34;
            }
          + node {
              + role = &#34;worker&#34;
            }
        }
    }

Plan: 1 to add, 0 to change, 0 to destroy.
</code></pre><p>Apply the changes by running <code>terraform apply</code></p>
<p>This command executes the actions proposed in a Terraform <strong>plan</strong>.</p>
<p>Once completed, your new <strong>Kubernetes</strong> cluster is created.</p>
<p>Let&rsquo;s do some interactions with our cluster.</p>
<p><code>kubectl cluster-info </code></p>
<p><code>Kubernetes control plane is running at https://127.0.0.1:53839</code></p>
<p><code>kubectl get nodes</code></p>
<pre tabindex="0"><code>NAME                        STATUS   ROLES           AGE     VERSION
kindcluster-control-plane   Ready    control-plane   3m48s   v1.24.0
kindcluster-worker          Ready    &lt;none&gt;          3m20s   v1.24.0
</code></pre><h2 id="conclusion">Conclusion</h2>
<p>In this blog post, we installed Kubernetes (kind) using Terraform.</p>
<p>In the next post, we will create a <strong>Kube-Prometheus stack</strong> using a <strong>Terraform</strong> module.</p>
]]></content>
        </item>
        
        <item>
            <title>Hugo</title>
            <link>https://magsther.github.io/posts/hugo/</link>
            <pubDate>Fri, 08 Jul 2022 20:16:37 +0200</pubDate>
            
            <guid>https://magsther.github.io/posts/hugo/</guid>
            <description>Hugo in 5 minutes Hugo is a static site generator written in Go.
Static generators like Hugo let&amp;rsquo;s you focus on writing content.
What about Wordpress? Done that.. What about building a blog using Django? Done that as well.
A big advantage of using Hugo is that I don&amp;rsquo;t need need to mess around with hosting. GitHub provides free and fast static hosting over SSL and at the same time automates the whole process.</description>
            <content type="html"><![CDATA[<h2 id="hugo-in-5-minutes">Hugo in 5 minutes</h2>
<p><a href="https://gohugo.io/">Hugo</a> is a static site generator written in Go.</p>
<p>Static generators like <a href="https://gohugo.io/">Hugo</a> let&rsquo;s you focus on <strong>writing content</strong>.</p>
<p>What about <strong>Wordpress</strong>? Done that.. What about building a blog using <strong>Django</strong>? Done that as well.</p>
<p>A big advantage of using <strong>Hugo</strong> is that I don&rsquo;t need need to mess around with hosting.
<a href="https://github.com">GitHub</a> provides <strong>free</strong> and <strong>fast</strong> static hosting over <strong>SSL</strong> and at the same time <strong>automates</strong> the whole process.
That&rsquo;s a win, win and win:)</p>
<p><strong>Hugo</strong> works almost out of the box, but still gives me the possibility to modify things if needed.</p>
<h2 id="deploy-a-hugo-blog-in-less-than-10-steps">Deploy a Hugo blog in less than 10 steps</h2>
<ol>
<li>Get a <a href="%22https://www.github.com%22">GitHub</a> Account</li>
<li>Create a GitHub repository and name it <code>&lt;username&gt;.github.io.</code></li>
<li>Create a Hugo project <code>hugo new site . --force</code></li>
<li>Pick a (Hugo) theme from <a href="https://themes.gohugo.io/">here</a></li>
<li>Add content to your new blog. <code>hugo new posts/hugo.md</code></li>
<li>Generate the blog by running <code>hugo</code></li>
<li>Push to GitHub</li>
<li>Enable <strong>GitHub Pages</strong></li>
<li>Visit your published blog at https://<!-- raw HTML omitted -->.github.io</li>
</ol>
]]></content>
        </item>
        
    </channel>
</rss>
